# TapChief
TapSearch is a method in which one can provide a document and index it and divide into multiple paragraph on the basis of two consecutive new line character. One can search the paragraphs that contain a particular word and rearrange those paragraphs(each paragraph have a unique id) on the basis of the frequency of that word.
